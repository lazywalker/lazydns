# Downloader Plugin + Cron Scheduling
#
# This configuration demonstrates a lightweight, plugin-based approach
# for automatically downloading and updating DNS rule files.
#
# Architecture:
#   1. CronPlugin triggers on schedule (every 6 hours: 00:00, 06:00, 12:00, 18:00 local time)
#   2. CronPlugin invokes the Downloader plugin via invoke_plugin action
#   3. Downloader downloads files and updates them atomically
#   4. DomainSet/IPSet/Hosts automatically detect changes and reload
#
# Logging:
#   - CronPlugin outputs DEBUG logs for job scheduling and execution
#   - DownloaderPlugin outputs detailed logs for each file download
#   - Set log level to debug or trace to see detailed operation logs
#

# ============================================================================
# ADMIN API CONFIGURATION
# ============================================================================

admin:
  # Enable admin API for runtime management
  # Admin API provides endpoints for:
  #   - /api/cache/control - Cache management (clear, stats)
  #   - /api/config/reload - Config hot-reload
  #   - /api/server/stats - Server status
  enabled: true
  # Listen address (default: 127.0.0.1:8000)
  # Warning: Do NOT expose to untrusted networks!
  # Use a reverse proxy with authentication in production
  addr: "127.0.0.1:8000"

metrics:
  # Enable monitoring server for Prometheus metrics and health checks
  enabled: true
  # Listen address (default: 127.0.0.1:8001)
  addr: "127.0.0.1:8001"

# ============================================================================
# WEBUI CONFIGURATION
# ============================================================================

web:
  # Enable WebUI dashboard for real-time monitoring
  enabled: true
  # Listen address (default: 127.0.0.1:8002)
  listen: "0.0.0.0:8002"
  # Path to static files directory (WebUI frontend)
  # Comment out or remove to use embedded assets when compiled with web-embed feature
  # static_dir: "../../webui/dist"
  # Event bus capacity for real-time streaming
  event_bus_capacity: 1024
  # Metrics collection configuration
  metrics:
    window_secs: 300  # 5 minutes sliding window
    top_n: 10         # Track top 10 domains/clients
  # SSE configuration
  sse:
    keepalive_secs: 30
    max_connections: 100
  # WebSocket configuration  
  websocket:
    max_connections: 50
  # CORS configuration (for development)
  cors:
    allowed_origins: []  # Empty = allow all
  alerts:
    enabled: true
    dedup_window_secs: 60
    max_alerts: 100
    rules:
      # Alert when rate limiting triggers
      - name: "Rate Limit Exceeded"
        condition:
          type: security_event
          event_type: "rate_limit_exceeded"
        severity: warning
        message: "Rate limit exceeded for {client_ip}"
      
      # Alert when domain is blocked
      - name: "Blocked Domain Query"
        condition:
          type: security_event
          event_type: "blocked_domain_query"
        severity: warning
        message: "Query blocked for {domain} from {client_ip}"
      
      # Alert when upstream DNS fails
      - name: "Upstream DNS Failure"
        condition:
          type: security_event
          event_type: "upstream_failure"
        severity: critical
        message: "Upstream DNS server failed"
      
      # Alert when ACL denies access
      - name: "ACL Denied"
        condition:
          type: security_event
          event_type: "acl_denied"
        severity: warning
        message: "ACL denied query from {client_ip} for {domain}"
      
      # Alert when query is malformed
      - name: "Malformed Query"
        condition:
          type: security_event
          event_type: "malformed_query"
        severity: info
        message: "Malformed DNS query from {client_ip} for {domain}"
      
      # Alert when query times out
      - name: "Query Timeout"
        condition:
          type: security_event
          event_type: "query_timeout"
        severity: warning
        message: "Query timeout for {domain}"
    

# ============================================================================
# PLUGINS CONFIGURATION
# ============================================================================

plugins:
  # ========================================================================
  # Data Providers - Load DNS rule files
  # ========================================================================

  # Load domain blocklists (Ad, etc.)
  - tag: domain_reject_list
    type: domain_set
    args:
      files:
        - "reject-list.txt"
      auto_reload: true # Automatically reload when file changes

  # Load domain proxy list
  - tag: domain_proxy_list
    type: domain_set
    args:
      files:
        - "proxy-list.txt"
        - "proxy-ext-list.txt"
      auto_reload: true

  # Load domain direct list
  - tag: domain_direct_list
    type: domain_set
    args:
      files:
        - "direct-list.txt"
        - "apple-cn.txt"
        - "my-domain-list.txt"
      auto_reload: true

  # Load IP direct list
  - tag: ip_direct_list
    type: ip_set
    args:
      files:
        - "china-ip-list.txt"
        - "white-ip-list.txt"
      auto_reload: true

  # Load hosts files
  - tag: hosts_files
    type: hosts
    args:
      files:
        - "hosts.txt"
        - "hosts-github.txt"
      auto_reload: true

  # ========================================================================
  # Cron Scheduler - Triggers downloader on schedule
  # ========================================================================

  - tag: auto_update_scheduler
    type: cron
    args:
      jobs:
        - name: auto_update
          # Cron expression: minute hour day month weekday
          # Note: cronexpr uses the standard crontab order (minute hour day month weekday)
          # Examples:
          #   "5 2 * * *" = every day at 02:05 (local time)
          #   "0 */6 * * *" = every 6 hours at minute 0 (00:00, 06:00, 12:00, 18:00 local time)
          #   "0 * * * *" = every hour at minute 0
          # You may optionally append a TZ name as the last token: e.g. "0 */6 * * * UTC"
          cron: "10 2  * * *"
          action:
            invoke_plugin:
              type: "downloader"
              args:
                files:
                  - url: "https://raw.githubusercontent.com/Loyalsoldier/v2ray-rules-dat/release/reject-list.txt"
                    path: "reject-list.txt"
                  - url: "https://raw.githubusercontent.com/Loyalsoldier/v2ray-rules-dat/release/direct-list.txt"
                    path: "direct-list.txt"
                  - url: "https://raw.githubusercontent.com/Loyalsoldier/geoip/release/text/cn.txt"
                    path: "china-ip-list.txt"
                  - url: "https://raw.githubusercontent.com/Loyalsoldier/v2ray-rules-dat/release/proxy-list.txt"
                    path: "proxy-list.txt"
                  - url: "https://raw.githubusercontent.com/Loyalsoldier/v2ray-rules-dat/release/apple-cn.txt"
                    path: "apple-cn.txt"
                  - url: "https://raw.githubusercontent.com/lazywalker/ghosts/refs/heads/master/hosts.mdns"
                    path: "hosts-github.txt"
                timeout_secs: 30
                concurrent: false

  # ========================================================================
  # Cache, Redirect Plugins, and ROS Addrlist Updater
  # ========================================================================

  # ========================================================================
  #   * Cache with LazyCache Optimization
  # ========================================================================

  # Main cache plugin with lazycache enabled for hot entries
  # LazyCache automatically refreshes frequently accessed entries before
  # they expire, preventing cache misses and reducing query latency.
  #
  # How LazyCache works:
  #   1. When a cached entry is accessed (cache hit)
  #   2. If remaining TTL < lazycache_threshold (e.g., 5% of original TTL)
  #   3. The entry is marked for refresh in a separate query
  #   4. This keeps hot entries fresh without waiting for expiry
  #
  # Benefits:
  #   - Eliminates cache miss latency for hot domains
  #   - Reduces upstream DNS load by proactive refresh
  #   - Improves user experience with consistent response times
  - tag: cache
    type: cache
    args:
      # Maximum number of cached entries (default: 1024)
      size: 1024

      # Optional: Enable caching of error responses (NXDOMAIN, SERVFAIL, etc.)
      negative_cache: true
      # TTL for cached error responses (in seconds)
      negative_ttl: 300

      # Optional: Enable LazyCache optimization
      enable_lazycache: true
      # Refresh hot entries when TTL drops to 5% of original value
      # Example: entry with TTL=300, refresh when remaining < 15 seconds
      lazycache_threshold: 0.05

      # Optional: Enable stale-serving with extended cache TTL (in seconds)
      # Set `enable_lazycache` to false to test cache_ttl stale-serving
      # When DNS TTL expires but cache TTL remains, serve stale response with small TTL
      # and refresh in background. Set to desired cache duration (e.g., 600 for 10 minutes)
      cache_ttl: 600

      # TODO: Prefetch extension can be enabled here in future
      # Optional: Enable prefetch for additional performance
      # Prefetch is more aggressive than lazycache and refreshes at higher threshold
      # enable_prefetch: true
      # prefetch_threshold: 0.1  # Refresh at 10% remaining TTL

  # ========================================================================
  # Alternative Cache Configurations (Commented Out Examples)
  # ========================================================================

  # Example 1: Conservative cache (large size, high threshold)
  # Suitable for stable DNS environments with many unique domains
  # - tag: cache_conservative
  #   type: cache
  #   args:
  #     size: 5000  # Larger cache for more domains
  #     lazycache_threshold: 0.1  # Refresh at 10% TTL (more conservative)

  # Example 2: Aggressive cache (small size, low threshold)
  # Suitable for high-traffic environments with limited memory
  # - tag: cache_aggressive
  #   type: cache
  #   args:
  #     size: 512  # Smaller cache to save memory
  #     lazycache_threshold: 0.02  # Refresh at 2% TTL (more aggressive)

  # Example 3: Performance cache (without error caching)
  # Useful when you want to requery failed domains instead of caching errors
  # - tag: cache_positive_only
  #   type: cache
  #   args:
  #     size: 1024
  #     negative_cache: false  # Don't cache errors
  #     enable_lazycache: true
  #     lazycache_threshold: 0.05

  # ========================================================================
  # Audit Plugin - Query Logging and Security Events
  # ========================================================================
  # Global buffer and rotation settings apply to both query_log and security_events
  # unless overridden at the individual log level.
  
  - tag: audit
    type: audit
    args:
      # Enable audit logging globally (default: false)
      enabled: true

      # Enable writing logs to filesystem (default: true)
      # When false: only event bus publishing is active (WebUI displays real-time data)
      # When true: both filesystem writes AND event bus publishing are active
      # Note: Disabling file writes does NOT affect WebUI audit display (event bus is independent)
      log_to_file: false

      # Global log buffer size before flush (applies to both logs, default: 100)
      # Flush to disk after this many entries (trade-off between memory and I/O)
      buffer_size: 100

      # Global maximum file size before rotation (default: 100MB)
      # Supports human-readable sizes: 100K, 10M, 1G, etc.
      max_file_size: 100M

      # Global number of rotated files to keep (default: 10)
      # Older files are deleted when count exceeds this limit
      max_files: 10

      # Query logging configuration
      query_log:
        # Log file path - relative to the working directory when lazydns starts
        # When running with "-d examples/etc/", paths are relative to examples/etc/
        path: log/lazydns/queries.log

        # Output format: "json" or "text" (default: json)
        format: json

        # Sampling rate (0.0 to 1.0): fraction of queries to log (default: 1.0)
        # Use 0.1 to log 10% of queries, reducing I/O overhead on high-traffic servers
        sampling_rate: 1.0

        # Include response details in log entries (default: true)
        include_response: true

        # Include client IP in log entries (default: true)
        include_client_ip: true

        # Exclude DNS-SD discovery queries (default: true)
        # Filters out queries matching _dns-sd._udp patterns to reduce noise
        # DNS-SD queries are typically internal discovery queries (e.g., b._dns-sd._udp.*.in-addr.arpa)
        # Set to false to log all queries including DNS-SD discovery
        exclude_dns_sd: true

        # Optional: Override global log_to_file setting for this log only
        # log_to_file: true

        # Optional: Override global buffer_size for this log only
        # buffer_size: 50

        # Optional: Override global max_file_size for this log only
        # max_file_size: 50M

        # Optional: Override global max_files for this log only
        # max_files: 5

      # Security event logging configuration
      security_events:
        # Enable security event logging (default: true when present)
        enabled: true

        # Log file path - relative to working directory
        path: log/lazydns/security.log

        # Events to track (empty list = log all events)
        events:
          - rate_limit_exceeded       # Client exceeded query rate limit
          - blocked_domain_query      # Query for a blocked domain
          - upstream_failure          # Upstream DNS server failed
          - acl_denied                # Query denied by ACL rules
          - malformed_query           # Malformed DNS query
          - query_timeout             # Query timeout occurred

        # Include full query details in security event logs (default: true)
        include_query_details: true

        # Optional: Override global log_to_file setting for this log only
        # log_to_file: true

        # Optional: Override global buffer_size for security events only
        # buffer_size: 50

        # Optional: Override global max_file_size for security events only
        # max_file_size: 50M

        # Optional: Override global max_files for security events only
        # max_files: 5

  # redirect plugin for specific domains
  - tag: redirect
    type: redirect
    args:
      rules:
        - www.cnbeta.com www.cnbeta.com.cdn.cloudflare.net

  # ROS Addrlist updater for gfwlist IPs
  - tag: add_gfwlist
    type: ros_addrlist
    args:
      addrlist: "lazydns-gfwlist"
      server: "http://192.168.88.1:80"
      user: "lazydns"
      passwd: "Lazyisgood"
      mask4: 24
      mask6: 32

  # ========================================================================
  # Upstream Servers
  # ========================================================================

  # forward to domestic DNS, concurrent queries
  - tag: upstream_direct
    type: forward
    args:
      concurrent: 2
      upstreams:
        - addr: udp://119.29.29.29 # Tencent Public DNS
        - addr: udp://223.5.5.5 # AliDNS Public DNS
      health_checks: true
      max_attempts: 3

  # forward to proxy DNS, concurrent queries
  - tag: upstream_proxy
    type: forward
    args:
      concurrent: 2
      upstreams:
        - addr: tcp://8.8.8.8 # Google Public DNS
        - addr: tcp://1.1.1.1 # Cloudflare DNS
      health_checks: true
      max_attempts: 3

  # blackhole upstream for rejected domains
  - tag: upstream_reject
    type: blackhole

  # rate limiter plugin
  - tag: rate_limiter_default
    type: rate_limit
    args:
      max_queries: 1000
      window_secs: 60

  # domain validator plugin
  - tag: validator
    type: domain_validator
    args:
      cache_size: 1000
  
  # ========================================================================
  # Sub sequence and Fallback Logic
  # ========================================================================

  # direct sub-sequence, accpt domestic IPs, drop others
  - tag: sequence_direct
    type: sequence
    args:
      - exec: $upstream_direct
      - matches: "!resp_ip $ip_direct_list"
        exec: drop_resp
      - exec: accept

  # proxy sub-sequence
  - tag: sequence_proxy
    type: sequence
    args:
      - exec: $upstream_proxy
      - exec: accept

  # sub-sequence for gfwlist,
  # adding IPs to some list(by exec ROS updater) before accepting when not domestic
  - tag: sequence_gfwlist
    type: sequence
    args:
      - exec: ttl 300-3600
      - matches: "!resp_ip $ip_direct_list"
        exec: $add_gfwlist
      - exec: accept

  # fallback sequence
  - tag: fallback
    type: fallback
    args:
      primary: sequence_direct
      secondary: sequence_proxy
      threshold: 500
      always_standby: true

  # ========================================================================
  # Main sequence - the core DNS processing logic
  # ========================================================================

  # main sequence
  - tag: sequence_main
    type: sequence
    args:
      - exec: $validator
      - exec: $rate_limiter_default
      
      # check hosts files first
      - exec: $hosts_files
      - matches: has_resp
        exec: accept

      # reject PTR and DNSSEC to fix some iOS issues
      - matches: qtype 12 65
        exec: reject 0

      # block ads
      - matches: qname $domain_reject_list
        # demonstration: use inline quick-setup to blackhole
        exec: black_hole 127.0.0.1
        # or use the upstream_reject defined above
        # exec: $upstream_reject

      - exec: $redirect

      # skip cache for dynamic domains, others use cache
      - matches: "!qname 00006801.com"
        exec: $cache
      - matches: has_resp
        exec: accept

      # domestic domains
      - matches: qname $domain_direct_list
        exec: $upstream_direct
      - matches: has_resp
        exec: accept

      # foreign domains
      - matches: qname $domain_proxy_list
        exec: $upstream_proxy
      - matches: has_resp
        exec: jump $sequence_gfwlist

      # other unknown domains
      - exec: $fallback

  # ========================================================================
  # Server Configurations, listen on multiple protocols and ports
  # ========================================================================

  # UDP server (built-in)
  - tag: udp_server
    type: udp_server
    args:
      entry: sequence_main
      listen: :5354

  # TCP server (built-in)
  - tag: tcp_server
    type: tcp_server
    args:
      entry: sequence_main
      listen: :5354

  # DNS over HTTPS (DoH) server (requires tls and doh features)
  - tag: doh_server
    type: doh_server
    args:
      entry: sequence_main
      # use unprivileged port for examples; change to :443 for production
      listen: :8443
      # Paths to certificate and private key PEM files (relative to this config dir)
      cert_file: certs/cert.pem
      key_file: certs/key.pem

  # DNS over TLS (DoT) server (requires tls and dot features)
  - tag: dot_server
    type: dot_server
    args:
      entry: sequence_main
      # standard DoT port is 853; use 8853 in examples if you need non-root port
      listen: :8853
      # Paths relative to this config directory
      cert_file: certs/cert.pem
      key_file: certs/key.pem

  # DNS over QUIC (DoQ) server (requires tls and doq features)
  - tag: doq_server
    type: doq_server
    args:
      entry: sequence_main
      # standard DoQ port is 784; use 8784 in examples if you need non-root port
      listen: :8784
      # Paths relative to this config directory
      cert_file: certs/cert.pem
      key_file: certs/key.pem

# ============================================================================
# LOGGING CONFIGURATION
# ============================================================================

log:
  # * trace | debug | info |warn | error
  level: debug

  # * text (by default) | json
  # format: text

  # * true | false (by default)
  console: true

  # File logging configuration
  file:
    # Enable file logging (default: false)
    enabled: false
    
    # Path to log file
    path: /var/log/lazydns/lazydns.log
    
    # Rotation configuration
    rotation:
      # Rotation type: time, size, both, or never
      type: size
      
      # Time period: daily or hourly (for type: time or both)
      period: daily
      
      # Max file size in bytes (for type: size or both)
      max_size: 10M   # 10MB
      
      # Number of rotated files to keep (for type: size or both)
      max_files: 5
      
      # Compress rotated files (future feature)
      compress: false

# ============================================================================
# END OF CONFIGURATION
# ============================================================================

# ============================================================================
# FILE UPDATER SCHEDULE DETAILS
# ============================================================================
#
# The scheduler will trigger at:
#   - Time: 02:05 UTC every day
#   - Action: Download all files in the files list
#   - Download Mode: Sequential (one at a time) to avoid bandwidth spikes
#   - Timeout: 30 seconds per file
#   - Retry: Built-in retry on network errors
#
# After files are downloaded:
#   1. Files are written to temporary locations
#   2. Atomically renamed to final paths (no partial files)
#   3. File watches detect the change automatically
#   4. DomainSet/IPSet/Hosts reload the new content
#   5. DNS queries immediately use updated rules
#
# Performance Notes:
#   - With 6 files and sequential mode: ~6-12 seconds typical (varies by file size)
#   - Concurrent mode: ~2-3 seconds but higher bandwidth usage
#   - File reloads happen in background, no DNS query interruption
#   - Failed downloads logged but don't prevent DNS operation
#
# For production deployments:
#   - Monitor logs for download failures: grep "download failed" logs/lazydns.log
#   - Verify file sizes after update: ls -la *.txt
#   - Consider downloading during off-peak hours (e.g., 03:05 UTC instead of 02:05)
#   - Set timeout_secs based on file sizes and network speed
